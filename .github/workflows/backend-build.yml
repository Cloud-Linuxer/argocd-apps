name: Backend Build and Push

on:
  push:
    paths:
      - 'backend/**'
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    
    - name: Build with Docker
      run: |
        cd backend
        docker build -t 10.43.6.227:5000/library/backend:${{ github.sha }} .
        docker build -t 10.43.6.227:5000/library/backend:latest .
    
    - name: Push to Harbor
      run: |
        docker login 10.43.6.227:5000 -u admin -p Harbor12345
        docker push 10.43.6.227:5000/library/backend:${{ github.sha }}
        docker push 10.43.6.227:5000/library/backend:latest
