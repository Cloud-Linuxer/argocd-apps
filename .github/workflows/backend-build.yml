name: Backend Build and Push
on:
  push:
    paths: ['backend/**']
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Get version from app.py
        id: version
        run: |
          VERSION=$(grep -o '"version": "[^"]*"' backend/app.py | cut -d'"' -f4)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx (host network)
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            network=host

      - name: Build and push (GHCR)
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/argocd-apps/backend:${{ steps.version.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/argocd-apps/backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
