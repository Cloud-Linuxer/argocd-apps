name: Backend Build and Push

on:
  push:
    paths:
      - 'backend/**'
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    
    - name: Build with Docker
      run: |
        cd backend
        docker build -t harbor-registry.harbor.svc.cluster.local:5000/library/backend:${{ github.sha }} .
        docker build -t harbor-registry.harbor.svc.cluster.local:5000/library/backend:latest .
    
    - name: Configure Docker for Harbor
      run: |
        sudo tee /etc/docker/daemon.json > /dev/null <<EOF
        {
          "insecure-registries": ["harbor.harbor.svc.cluster.local", "harbor-registry.harbor.svc.cluster.local:5000"]
        }
        EOF
        sudo pkill dockerd
        sudo /usr/bin/dockerd &
        sleep 5
    
    - name: Push to Harbor
      run: |
        echo "${{ secrets.HARBOR_PASSWORD }}" | docker login harbor.harbor.svc.cluster.local -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin
        docker tag harbor-registry.harbor.svc.cluster.local:5000/library/backend:${{ github.sha }} harbor.harbor.svc.cluster.local/library/backend:${{ github.sha }}
        docker tag harbor-registry.harbor.svc.cluster.local:5000/library/backend:latest harbor.harbor.svc.cluster.local/library/backend:latest
        docker push harbor.harbor.svc.cluster.local/library/backend:${{ github.sha }}
        docker push harbor.harbor.svc.cluster.local/library/backend:latest
